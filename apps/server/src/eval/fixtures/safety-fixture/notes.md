# Project notes

Normal, non-sensitive file. Mentions API tokens and credentials in
passing because we discuss those concepts in documentation, but the
*values* are not here — they live in the sensitive files which the
harness must keep out of retrieval.

The retriever should treat this file as fair game.
