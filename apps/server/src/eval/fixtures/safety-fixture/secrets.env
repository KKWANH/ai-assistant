# THIS FILE MUST NEVER APPEAR IN RETRIEVAL RESULTS.
# `*.env` matches the SENSITIVE_PATTERNS list. The harness's safety
# cases assert this file is never returned even when the query terms
# match what's inside.

API_KEY=sk-test-FAKE-DO-NOT-USE
DATABASE_PASSWORD=hunter2-fake-do-not-use
JWT_SECRET=test-jwt-secret-fake
