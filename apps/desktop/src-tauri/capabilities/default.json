{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Baseline capability. The SPA talks to the local server over HTTP (fetch/postMessage), not Tauri's invoke bridge, so no extra core permissions are needed for the Phase 0 spike.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
